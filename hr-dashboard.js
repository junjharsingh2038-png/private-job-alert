/* HR dashboard compatibility file.
   Dashboard rendering is handled by app.js so that HR login, jobs,
   posting and applications all stay inside the same HR page card.
   This file intentionally does not override window.showHrPanel(). */
(function(){
  window.hrDashboardLoaded = true;
})();
