(function(){'use strict';
function esc(v){return String(v??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]))}
function getText(card,sel){const e=card.querySelector(sel);return e?e.textContent.trim():''}
function getJob(card){const b=card.querySelector('.apply[onclick]');if(b){const s=b.getAttribute('onclick')||'',a=s.indexOf('openApply('),z=s.lastIndexOf(')');if(a>=0&&z>0){try{return JSON.parse(s.slice(a+10,z).replace(/&#39;/g,"'"))}catch(e){}}}return {title:getText(card,'h3'),company:getText(card,'.company'),description:getText(card,'.nk-description'),location:getText(card,'.nk-meta span'),qualification:'Any',job_type:'Full Time',salary:'As per company'} }
function show(j){if(window.openJobDetails){window.openJobDetails(j);return}const modal=document.getElementById('modal'),box=document.getElementById('modalContent');if(!modal||!box)return;box.innerHTML='<div class="job-description-view"><button class="close" onclick="closeModal()">×</button><h2>'+esc(j.title||'Job')+'</h2><p><b>'+esc(j.company||j.company_name||'')+'</b></p><p>📍 '+esc(j.location||'India')+'</p><hr><h3>Job Description</h3><div>'+esc(j.description||'No detailed description available.').replace(/\n/g,'<br>')+'</div></div>';modal.classList.remove('hidden')}
function add(){document.querySelectorAll('.nk-job-card').forEach(card=>{if(card.querySelector('.nk-view-details'))return;const actions=card.querySelector('.nk-actions');if(!actions)return;const b=document.createElement('button');b.type='button';b.className='nk-view-details';b.textContent='View Details';b.setAttribute('aria-label','View full job description');b.onclick=e=>{e.preventDefault();e.stopPropagation();show(getJob(card))};actions.insertBefore(b,actions.firstChild)})}
function start(){add();setTimeout(add,250);setTimeout(add,700);setTimeout(add,1500);setTimeout(add,3000);if(document.body)new MutationObserver(add).observe(document.body,{childList:true,subtree:true})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();window.PJAJobView={add,show};})();