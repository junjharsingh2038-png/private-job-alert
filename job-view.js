(function(){'use strict';
function getJob(card){const b=card.querySelector('.apply[onclick]');if(!b)return null;const s=b.getAttribute('onclick')||'',a=s.indexOf('openApply('),z=s.lastIndexOf(')');if(a<0||z<0)return null;try{return JSON.parse(s.slice(a+10,z).replace(/&#39;/g,"'"))}catch(e){return null}}
function esc(v){return String(v??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]))}
function show(j){if(window.openJobDetails){window.openJobDetails(j);return}const modal=document.getElementById('modal'),box=document.getElementById('modalContent');if(!modal||!box)return;box.innerHTML='<div class="job-description-view"><button class="close" onclick="closeModal()">×</button><h2>'+esc(j.title||'Job')+'</h2><p><b>'+esc(j.company||j.company_name||'')+'</b></p><p>📍 '+esc([j.city,j.state].filter(Boolean).join(', ')||j.location||'India')+'</p><hr><h3>Job Description</h3><div>'+esc(j.description||'No detailed description available.').replace(/\n/g,'<br>')+'</div><button class="apply" style="margin-top:20px" onclick="closeModal();openApply('+JSON.stringify(j).replace(/'/g,"&#39;")+')">Apply Now</button></div>';modal.classList.remove('hidden')}
function add(){document.querySelectorAll('.nk-job-card').forEach(card=>{if(card.querySelector('.nk-view-details'))return;const j=getJob(card);if(!j)return;const actions=card.querySelector('.nk-actions');if(!actions)return;const b=document.createElement('button');b.type='button';b.className='nk-view-details';b.textContent='View Details';b.onclick=e=>{e.preventDefault();e.stopPropagation();show(j)};actions.insertBefore(b,actions.firstChild)})}
function start(){add();setTimeout(add,300);setTimeout(add,900);setTimeout(add,1800);new MutationObserver(add).observe(document.body,{childList:true,subtree:true})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();window.PJAJobView={add,show};})();