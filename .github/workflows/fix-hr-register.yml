name: Fix HR Register
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obsolete fixer disabled; HR auth V2 is active"