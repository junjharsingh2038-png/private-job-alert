name: Obsolete HR Profile Attachment
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Portal assets are managed by the unified attachment workflow."